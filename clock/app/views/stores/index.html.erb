<div class="container">
  <div class="row">
      <% @stores.each do |store| %>
        <div class="col-md-4">
          <div class="card mb-4 shadow-sm">
          <img src="https://picsum.photos/200/200" class="img-thumbnail" />
            <div class="card-body">
              <p class="card-text">
                <p><strong>Name:</strong> <%= store.name %></p>
                <p><strong>Store number:</strong> <%= store.id %></p>
              </p>
              <div class="d-flex justify-content-between align-items-center">
                <%= link_to 'Show', edit_store_path(store), class: 'btn btn-primary btn-lg btn-block' %>
              </div>
            </div>
          </div>
        </div>
      <% end %>
  </div>
</div>
